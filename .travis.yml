language: python
python: 3.6
services:
  - docker

script:
  - git clone git://github.com/ome/omero-test-infra .omero
  - .omero/docker lib

deploy:
  provider: pypi
  user: $PYPI_USER
  password: $PYPI_PASSWORD
  on:
    tags: true
